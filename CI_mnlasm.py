#!/usr/bin/python

import sys
import glob

watchlist = [];
hashlist = [];

def main():
	while True:
		time.sleep(5)
		for i in watchlist:
				


if __name__ == "__main__":
	for i in sys.argv[1:]:
		watchlist.extend(glob.glob(i));
	main()
